import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Authentication - Account Settings",
};

export default async function KitchensSettings() {
  return <div className="space-y-5"></div>;
}
